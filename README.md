# 🎬 **Movie Catalog — Консольное приложение**

Добро пожаловать в **Movie Catalog** — удобное консольное приложение для ведения списка фильмов.
Вы можете добавлять фильмы, отмечать их как просмотренные, искать по году и хранить каталог в JSON.

---

## 🎉 **Возможности**

| №   | Функция                              | Описание                                      |
| --- | ------------------------------------ | --------------------------------------------- |
| 1️⃣ | **Показать все фильмы**              | Выводит весь каталог с ID, годом и статусом.  |
| 2️⃣ | **Добавить фильм**                   | Добавляет новый фильм с уникальным ID.        |
| 3️⃣ | **Отметить фильм как просмотренный** | Позволяет отметить любое кино по ID.          |
| 4️⃣ | **Найти фильмы по году**             | Ищет все фильмы, выпущенные в указанном году. |
| 0️⃣ | **Выход**                            | Сохраняет каталог и завершает программу.      |

---

## 🏗 **Структура проекта**

```
movies/
│
├── app.py               # Консольный интерфейс (меню, ввод)
├── logic.py             # Вся бизнес-логика
├── movies.json          # База фильмов в JSON
├── README.md            # Описание проекта
│
└── tests/
    └── test_logic.py    # Юнит-тесты для функции logic.py
```

---

## ⚙️ **Логика (logic.py)**

Файл содержит функции:

* `load_movies(path)`
* `save_movies(path, movies)`
* `add_movie(movies, title, year)`
* `mark_watched(movies, movie_id)`
* `find_by_year(movies, year)`

Все функции:

✔ не выводят данные на экран
✔ не взаимодействуют с пользователем
✔ возвращают данные строго согласно спецификации

---

## 🧪 **Тесты (tests/test_logic.py)**

Директория `tests/` предназначена для автоматического тестирования логики приложения.
В файле `test_logic.py` можно проверить:

* корректное добавление фильма
* корректное изменение статуса просмотра
* корректную фильтрацию по году
* корректную загрузку и сохранение


---

## ▶️ **Запуск приложения**

```bash
python app.py
```

---

## ▶️ **Запуск тестов (если используешь pytest)**

```bash
pytest tests/
```

---

## ⭐ Особенности проекта

✔ Чёткое разделение логики и интерфейса
✔ JSON-хранение
✔ Проверка пользовательского ввода
✔ Соответствие требованиям задания
✔ Готовность к тестированию
